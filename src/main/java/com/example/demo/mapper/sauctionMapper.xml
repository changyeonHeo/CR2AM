<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="com.example.demo.mapper.sautionMapper">

   <select id="getSauctionCount" parameterType="String" resultType="int">
      select count(*) from sauction2 where id = #{id}
   </select>
   
</mapper>